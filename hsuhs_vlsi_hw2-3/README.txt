****************
README
****************
Note: the default path is /ONID_vlsi_hw folder: /hsuhs_vlsi_hw2

1. you can find all the [Filename].sv files in the default path or in the rtl_src folder.
2. you can find all the [Filename].do files in the default path or in the scripts folder.
3. all the [Filename].gate.v files are in the gate_src folder.
4. all the screen captures of waveform(rtl, gate and arbiter) are in the default path.
5. the answers.txt is in the default path.
6. I change the name of dv_script.txt to alu_dv_script.txt